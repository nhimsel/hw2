#include <iostream>

using namespace std;

int main()
{
    bool isSunny = true;
    bool isRaining = false;
    bool isWeekend = true;
    cout << "Sunny? " << isSunny << endl;
    cout << "Raining? " << isRaining << endl;
    if (isWeekend)
    {
        cout << "It's the weekend!" << endl;
    }
    

    return 0;
}